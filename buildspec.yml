version: 0.2
phases:
  install:
    commands:
      - command
      - command
    finally:
      - command
      - command
  pre_build:
    commands:
      - command
      - command
    finally:
      - command
      - command
  build:
    commands:
      - docker build -t learn-codebuild:${CODEBUILD_RESOLVED_SOURCE_VERSION} .
      - command
    finally:
      - command
      - command
  post_build:
    commands:
      - command
      - command
    finally:
      - command
      - command
# artifacts:
#   files:
#     - location
#     - location
#     - name
#   discard-paths: yes
#   base-directory: location
#   secondary-artifacts:
#     artifactIdentifier:
#       files:
#         - location
#         - location
#       discard-paths: yes
#       base-directory: location
#     artifactIdentifier:
#       files:
#         - location
#         - location
#       discard-paths: yes
#       base-directory: location
